interface:
  display_name: "MuseScore MCP"
  short_description: "Control an open MuseScore score through MCP"
  default_prompt: "Use $mcp-musescore to inspect the open MuseScore score and make the requested verified edits."
policy:
  allow_implicit_invocation: true
